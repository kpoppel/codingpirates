# Select what to run.  Just make a def main() in the program to run as a boot up program.
import asyncio
import main_server
asyncio.run(main_server.main())

#import main_client
#asyncio.run(main_client.main())